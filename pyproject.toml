[project]
name = "bnote"
version = "0.1.0"
description = "A simple note application with Python/bottle."
requires-python = ">=3.11"
dependencies = [
    "bottle==0.12.23",
    "python-dateutil==2.8.2",
]

[tool.ruff]
line-length = 88
